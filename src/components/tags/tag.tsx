export const Tag = ({ content }: { content: string }) => (
	<div className=" bg-primary dark:brightness-[0.9] text-light-sibao-1 text-xs font-normal px-2 py-[0.2rem] rounded-full">
		{content}
	</div>
);
